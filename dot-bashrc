#
# called interactive non-login:
# /etc/bash.bashrc (some Linux; not on Mac OS X) then ~/.bashrc
#
PS1='\[\e[1;32m\][\u@\h \W]\$\[\e[0m\] '
#
export EDITOR="mcedit"
export PGDATABASE=wbank
export PGUSER=wbowner
